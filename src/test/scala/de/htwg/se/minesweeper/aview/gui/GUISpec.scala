package de.htwg.se.minesweeper.aview.gui

import org.scalatest.matchers.should.Matchers._
import org.scalatest._
import org.scalatest.wordspec.AnyWordSpec





class GUISpec extends AnyWordSpec{
/*     "a gui" when {
        val testGame = new Game(Status.Playing)
        val feldy = Setup.prepare(testGame)
        val testController = Controller(feldy, testGame)
        var guiTest = new GUI(testController)
        guiTest.visible = false
        "initialised" should {
            "have a title" in {
                guiTest.title should be ("Minesweeper")
            }
        }
        guiTest.close()
    } */
    "a GUI" should {
        "run" in {
            
        }
        
    }
}